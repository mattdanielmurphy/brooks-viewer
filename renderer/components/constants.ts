import path from 'path'

export const PATH_TO_TRADING_COURSE_DATABASE_FILES = path.join(
	// __dirname,
	'../brooks-trading-course.com-market-update',
	'data',
)
export const PATH_TO_PRICE_ACTION_DATABASE_FILES = path.join(
	// __dirname,
	'../brooks-price-action.com-trading-updates',
	'data',
)
